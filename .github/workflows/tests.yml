name: Tests

on:
  push:
    branches:
      - main
      - andrej_dev
    paths-ignore:
      - 'coverage.svg'  # Prevent infinite loops when badge changes

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        python-version: ["3.10", "3.11", "3.12"]

    permissions:
      contents: read  # Only read access for test jobs

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install -r requirements_dev.txt
          pip install -e .[dev]

      - name: Run Tests
        run: |
          python -m pytest --cov=onepower --cov-config=.coveragerc \
            --cov-report xml:./coverage.xml --durations=25
        if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.11'

      - name: Run Tests (without coverage)
        run: |
          python -m pytest --durations=25
        if: matrix.os != 'ubuntu-latest' || matrix.python-version != '3.11'

      - name: Upload coverage report
        if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.11'
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage.xml

  update-badge:
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push'  # Don't run on PRs

    permissions:
      contents: write  # Needed to commit badge

    steps:
      - uses: actions/checkout@v4

      - name: Download coverage report
        uses: actions/download-artifact@v4
        with:
          name: coverage-report
          path: .

      - name: Generate coverage badge
        uses: tj-actions/coverage-badge-py@v2

      - name: Verify Changed files
        id: verify
        uses: tj-actions/verify-changed-files@v16
        with:
          files: coverage.svg

      - name: Commit and push badge
        if: steps.verify.outputs.files_changed == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add coverage.svg
          git commit -m "Updated coverage.svg [skip ci]"
          git push
